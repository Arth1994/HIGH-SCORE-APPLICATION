package com.example.arth.axs175430_asg5;import android.content.Context;import android.widget.Toast;import java.io.BufferedReader;import java.io.File;import java.io.FileOutputStream;import java.io.FileReader;import java.io.IOException;import java.io.PrintWriter;import java.util.ArrayList;/** This class is handles the reading and writing operations of the text file.* The name of the text file is CS6326.txt** */public class DataLayer {    public ArrayList<String[]> entries = new ArrayList<>();    public ArrayList<String> linesRead = new ArrayList<>();    public String filePathToRead = "";    /*    * Method to Write to the File    * */    public void writeToFile(ScoreModel scoreModel, Context applicationContext) {        File path = applicationContext.getFilesDir();        filePathToRead = path.toString();        File file =  new File(path, "CS6326.txt");        rewriteFile(filePathToRead+"/CS6326.txt");        Toast.makeText(applicationContext, filePathToRead+"/CS6326.txt", Toast.LENGTH_LONG).show();        try{            FileOutputStream fileOutputStream = new FileOutputStream(file);            fileOutputStream.write(scoreModel.toString().getBytes());            fileOutputStream.close();        }catch(IOException e){            e.printStackTrace();        }finally {        }    }    /*    * Function to Rewrite the File before a new entry is added into it    * */    private void rewriteFile(String s) {        try{            readFromFile();            PrintWriter printWriter = new PrintWriter(new FileOutputStream(s));            for(String entry:linesRead)            {                printWriter.println(entry);            }            printWriter.close();        }catch (Exception e){            e.printStackTrace();        }    }    /*    *Method to Read to the File    * */    public void readFromFile(){        try{            BufferedReader bufferedReader = new BufferedReader(new FileReader(filePathToRead+"/CS6326.txt"));            String lineRetrieved = null;            String[] words;            while(true)            {                lineRetrieved = bufferedReader.readLine();                linesRead.add(lineRetrieved);                if(lineRetrieved == null)                {                    break;                }else{                    //Splits the lines and adds them as String Array to the list of entries                    words = lineRetrieved.split("\t");                    entries.add(words);                }            }            bufferedReader.close();        }catch(Exception e){            e.printStackTrace();        }    }}